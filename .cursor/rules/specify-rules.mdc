---
description: Project Development Guidelines
globs: ["**/*"]
alwaysApply: true
---

# video-renderer Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-03-24

## Active Technologies
- N/A (in-memory rendering only) (001-video-renderer)
- TypeScript 5.9 (ES2020, NodeNext) + Vite 7, mp4box (demux), WebCodecs/Canvas/Web Audio (browser APIs) (001-webcodecs-video-adaptor)
- N/A (in-memory decoding; source from URL/File/Blob) (001-webcodecs-video-adaptor)
- TypeScript 5.9 (existing); React 18+ for demo app + Vite 7 (build + dev server), React, React DOM; Vega library (local `src/` or built `dist/`); existing: mp4box, @vitest/browser-playwrigh (002-vega-demo-github-pages)
- N/A (demo is client-only; upload is in-memory/session) (002-vega-demo-github-pages)
- TypeScript 5.9, ESM + mediabunny (replacing mp4box ^0.5.4), Vite 7.3 (002-multi-input-types)
- TypeScript 6.x (pnpm catalog; ES2020 library, ES2023 apps) + pnpm 10.x, Turborepo 2.7+, Vite 7.3.x / 8.x, Lit 3.3.x, VitePress 2.x alpha, TypeDoc 0.28.x, mediabunny ^1.40.0 (003-api-docs-demo)
- N/A (static sites, in-memory rendering) (003-api-docs-demo)

- TypeScript 5.9.x (latest 5.9.3) + Vite 7.3.x (library build), @biomejs/biome 2.3.x (format + lint), Vitest 4.x with browser mode (e.g. @vitest/browser-playwright), commitlint 20.x, husky 9.x, semantic-release 25.x (001-video-renderer)

## Project Structure

```text
src/
tests/
```

## Commands

npm test && npm run lint

## Code Style

TypeScript 6.x (workspace catalog): Follow standard conventions

## Recent Changes
- 003-api-docs-demo: TypeScript 6.x via pnpm catalog; pnpm 10.x, Turborepo 2.7+, Vite 7.3.x / 8.x, Lit 3.3.x, VitePress 2.x alpha, TypeDoc 0.28.x, mediabunny ^1.40.0
- 002-multi-input-types: Added TypeScript 5.9, ESM + mediabunny (replacing mp4box ^0.5.4), Vite 7.3
- 001-vega-demo-github-pages: Added TypeScript 5.9 (existing); React 18+ for demo app + Vite 7 (build + dev server), React, React DOM; Vega library (local `src/` or built `dist/`); existing: mp4box, @vitest/browser-playwrigh


<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
